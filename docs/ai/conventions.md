# Conventions

- Use existing React components and configuration constants as precedents; do not edit generated
  design-component runtime code.
- Verify real assets and external links after changes. Keep the layout usable at mobile and desktop
  widths, with readable copy and non-overlapping controls.
- Use `npm run typecheck` and `npm run build` from `react-app/` for source changes.